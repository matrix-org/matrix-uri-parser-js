{
  "name": "matrix-uri-parser",
  "version": "0.1.0",
  "description": "Decode the URL scheme for the Matrix chat protocol",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "matrix.org",
  "license": "Apache-2.0",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "impunity --entry-point src/index.ts --force-esm-dirs src/"
  },
  "devDependencies": {
    "impunity": "^1.0.1",
    "typescript": "^2.7.2"
  }
}
